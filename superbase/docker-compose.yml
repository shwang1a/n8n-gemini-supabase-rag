version: "3.9"

services:

  n8n:

    image: n8nio/n8n:latest

    container_name: n8n-rag

    restart: unless-stopped

    ports:
      - "5678:5678"

    env_file:
      - .env

    volumes:
      - ./data:/home/node/.n8n

    environment:
      - TZ=Asia/Taipei